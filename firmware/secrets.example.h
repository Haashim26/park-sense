// Copy this file to secrets.h and fill in your own values.
// secrets.h is gitignored and will NOT be committed.

#define WIFI_SSID "Wokwi-GUEST"
#define WIFI_PASSWORD ""

#define FIREBASE_HOST "park-sense-c0292-default-rtdb.asia-southeast1.firebasedatabase.app"

// Realtime Database > Project Settings > Service Accounts > Database secrets (legacy)
// Only needed if your DB rules require auth for writes (e.g. ".write": "auth != null")
#define FIREBASE_DB_SECRET "YOUR_DATABASE_SECRET"
